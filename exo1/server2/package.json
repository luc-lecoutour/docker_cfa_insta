{
  "name": "server2",
  "version": "1.0.0",
  "description": "server side",
  "main": "server",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "luc",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "expess": "^0.0.1-security",
    "express": "^4.18.2"
  }
}
